[server]
host = "127.0.0.1"
port = 8080

# Root directory tools (file_read, list_dir, find, search) are confined to.
# Set this to your project directory. Defaults to the current working directory
# if not set. Override with: GATEWAY_WORKSPACE_ROOT=/path/to/project
workspace_root = "/Users/preedee/Projects"

[log]
format = "json"
level = "info"
app_id = "nixacp-gateway"
app_version = "0.1.0"

[ollama]
url = "http://nixhome-linux-g1pro:11434"
default_model = "qwen2.5-coder:14b"
max_concurrent = 2

[[ollama.models]]
name = "qwen2.5-coder:14b"
display_name = "Qwen2.5 Coder 14B"
max_tokens = 16384
